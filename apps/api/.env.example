# Copy to .env and fill in. Never commit .env.
TYPESAFE_API_KEY=
# Stored runs are keyed by this name, so changing it re-judges every label.
TYPESAFE_MODEL=jev-latest
OPENFDA_API_KEY=
DATABASE_URL=sqlite:///rx_jev.db
